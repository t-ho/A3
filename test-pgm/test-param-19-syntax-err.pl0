type 
  subr = [1 ..10];
var x: int;

procedure p( a: int, b: boolean ) =
  begin
    x := a
  end;
begin
  x := 0;
  call p( , true );  // extra comma
  call p( 5, true, );  // extra comma
  write x
end
